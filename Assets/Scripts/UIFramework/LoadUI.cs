using UnityEngine;
using UnityEngine.UI;

namespace SaveSystemTutorial
{
    public class LoadUI : MonoBehaviour
    {
        #region Fields

        //[Header("-- Buttons --")]
        //[SerializeField] Button buttonSave;
        //[SerializeField] Button buttonLoad;

        #endregion

        void Start()
        {
            //buttonSave.onClick.AddListener(() => GameDataManger.Instance.Save());
            //buttonLoad.onClick.AddListener(() => GameDataManger.Instance.Load());
        }

        void Update()
        {

        }
    }
}